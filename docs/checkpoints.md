# Session Checkpoints

A running log of what was done each session, written when Karl says "end of session." Newest entry at the bottom.

---

## 2026-07-02

- Sorted "currently watching" cards by percent remaining; marked Samurai Champloo finished.
- Added poster images for all shows (MyAnimeList/Jikan for anime, TMDB for live-action), plus a pin/prioritize feature (max 3, sorts to top) via a 3-task subagent-driven-development plan.
- Verified every show's episode/season totals against MyAnimeList and corrected 5: Hell's Paradise, Classroom of the Elite, Fire Force, Apothecary Diaries (Season 2 was missing entirely), Kaiji. Updated `CLAUDE.md` so future episode-count lookups query Jikan/TMDB directly instead of a generic web search.
- Removed the "+1 ep" quick-bump button (added no real value over asking Claude directly, per feedback).
- Added a second "Tier List" tab: drag-and-drop S/A/B/C/D/F tiers plus a separate Ranked List mode, both backed by `localStorage`. Seeded the backlog with 72 previously-watched shows (posters fetched from MAL/TMDB) in a new `data.json` `"watched"` array, merged with the existing "completed" shows.
- Fixed mobile touch support: replaced native HTML5 drag-and-drop (no touch equivalent) with the Pointer Events API. Found and fixed a real bug along the way — reparenting the dragged element mid-drag broke pointer capture and silently dropped the `pointerup` event (documented in project memory: `pointer-events-reparent-bug.md`).
- Added a "Copy Export" button (tier list / ranked list to clipboard as text), since both are `localStorage`-only with no other backup path.
- Added tap-to-pick / tap-to-place for the tier list (solves dragging into a tier that's scrolled off-screen), up/down arrow buttons for the ranked list (solves imprecise drag-to-position across 72 items), and auto-scroll near the viewport top/bottom edges during any drag.
- Captured 2 memories from this session: a feedback note on skipping the full `subagent-driven-development` pipeline for small solo projects, and the Pointer Events reparent-bug technical note.

## 2026-07-02 (cont.)

- Brainstormed tier-list feature ideas (Karl asked "any other feature ideas?"); shortlisted an Anilist/MAL score badge as the pick to build.
- Implemented it: fetched MAL scores for all 72 tier-list pool shows via Jikan, with year/exact-title disambiguation to avoid remake/franchise mismatches (caught and fixed bad matches for Berserk 1997 vs. the 2016 remake, One Piece TV vs. a movie, and JoJo's Bizarre Adventure 2012 vs. a 1993 OVA). Added `malScore` to `data.json` and a score-badge UI element to both the Tier List and Ranked List views. Verified visually via Playwright — worked correctly.
- Karl reviewed it live and didn't like it ("i dont really vibe with it"). Reverted both commits (`git revert`) to remove the feature entirely; `data.json`/`index.html` are back to pre-feature state.
- Captured 1 feedback memory: for effort-heavy or visually-opinionated features, show a quick low-fidelity preview before the full build — even when told "implement without input needed," since that grants a decision-checkpoint skip, not an aesthetic one.

## 2026-07-23

- Bumped progress across several shows: Moriarty the Patriot to ep 8, then ep 19/24 (clarified "5 to go" meant overall, not season 1), then finished; Dragon Ball (Fan Cut) to ep 45, then 55, then 65 of 82.
- Marking Moriarty finished surfaced a data-model gap: it was moved out of `watching` entirely, which meant it dropped out of the app's "Completed" section (that section is derived from `watching` entries where `currentEpisode >= totalEpisodes`, not from the `watched` array — confirmed by checking how Samurai Champloo/Witch Hat Atelier still appear there). Fixed by keeping the entry in `watching` maxed out *and* adding it to `watched`.
- Codified this as a new rule in `CLAUDE.md`: finishing a show updates it in `watching` to totalEpisodes (drives the Completed section) and also adds it to `watched` (drives the tier-list grid) — never remove it from `watching`. No backfill needed for shows already finished before this rule.
- Investigated a reported "Moriarty poster missing" issue: verified the `posterUrl` against Jikan's API (exact match) and confirmed the image loads (200 OK) — likely a stale screenshot/cache, no data fix needed.
- Ran `/capture-workflow`: no new promotable memories this session — the finished-show rule went straight into project `CLAUDE.md` rather than through memory first.

## 2026-08-12

- Finished Loki (S2E12/12, added to `watched`); bumped Dragon Ball Z Kai (Recut) to ep 17/47.
- Split the Tier List into Anime/Western/Movies sub-tabs, each with independent tier placement and ranked-list state (separate `localStorage` keys, one-time migration of existing placements into the Anime tab). Added a `category` field to every `watching`/`watched` entry and backfilled all ~90 existing entries (movies bucketed by format regardless of origin — anime films like *Suzume* are `"movie"`, not `"anime"`).
- Built four more features in one pass (design spec first, then direct implementation — skipped the full plan/subagent pipeline given project scale, per existing memory): half-star (0–5, Letterboxd-style) ratings for movies; freeform notes on any watched show (Completed cards, Tier List chips, and the new Movies tab all share one note-editing modal); a dedicated Movies tab (poster grid, sortable by rating/A–Z/added); a Stats tab (episode/hour totals, genre/studio/category breakdowns, with explicit captions where the underlying data is partial); and sort/filter controls (% complete, last updated, A–Z, genre, studio) on both Watching and Completed. Ratings/notes live in `localStorage` only, synced to `data.json` via a new "Copy Ratings & Notes" export button (same paste-to-Claude pattern as the existing tier-list export) — `CLAUDE.md` now documents how to merge that block in.
- Researched real per-episode/per-film runtimes (web search) for the 3 western shows and 13 movies to back the Stats tab's hours-watched figure; anime uses a flat 20 min/episode assumption since it's not tracked per-show.
- Caught a design gap myself before building: notes were originally scoped to "Completed section + Movies tab" only, which would've left the ~60 older `watched`-only anime entries (never passed through `watching`) with no way to get a note at all, since they only ever render as Tier List chips. Fixed by adding the same note affordance to chips too, with care to stop the note icon's `pointerdown` from bubbling into the chip's own drag/pick handler.
- Follow-up fix: pinned shows in the Watching tab now always lead the list and are exempt from genre/studio filters — filters only narrow down the non-pinned remainder.
- Tested everything live via Playwright (star half-fill precision, note modal on all three surfaces, sort/filter persistence, drag-and-pick unaffected by the new note icon, export clipboard contents, stats math, pinned-first-with-filter behavior) — all passed.
- Ran `/capture-workflow`: no new promotable memories this session — nothing was written to `collaboration-preferences.md` or project memory to promote, and this session's decisions (notes-coverage fix, pinned-filter behavior) are watch-tracker-specific rather than repeatable cross-project patterns.

## 2026-08-12 (cont.)

- Removed the note button from Tier List chips per feedback (notes stay on Completed cards and the Movies tab only).
- Discussed the overall system: explained the current architecture (static `index.html` + `data.json` as source of truth, Claude-mediated writes, `localStorage` for everything interactive), confirmed test coverage and its gaps (no real touch-drag testing, no automated regression suite, ratings/notes only durable after an export), and talked through whether a backend makes sense — landed on "not yet," with a lightweight Supabase option identified as the right shape if/when the Claude-mediated flow becomes actual friction.
- Shipped 5 UX fixes in one pass: pin limit 3 → 5 (list has grown to 15 shows); a live "unsynced" count on the "Copy Ratings & Notes" button so data-loss risk is visible before it happens; a search/filter box for the Tier List (dims non-matching chips across tiers + pool + ranked list, doesn't hide them so placement stays visible); a search box on the Movies tab; a read-only ★ rating badge on Tier List movie chips so the two separate "movie rating" surfaces (absolute stars vs. relative S–F tier) feel connected; and auto-refresh on window focus so a manual Reload click usually isn't needed anymore.
- Gave a concrete backend effort estimate when asked: ~2–4h for the narrow scope (progress bump + mark-finished writable in-page via Supabase), ~half a day for the fuller scope (also migrating ratings/notes off `localStorage`). Recommended keeping "add a new show" Claude-mediated regardless, since that step benefits from judgment (remake/sequel disambiguation) not just a database write.
- Created `todo.md` (didn't exist before) to hold two deferred items for next session: (1) the Supabase backend work above, and (2) renaming the "Movies" tab to "Ratings" and extending the same rating/notes UI to Anime and Western shows, not just Movies — flagged as needing its own design pass since the data model currently treats `rating` as movie-only.
- Ran `/capture-workflow` again: still nothing new to promote — no writes to `collaboration-preferences.md` or project memory this half of the session either.

## 2026-08-14

- Renamed the "Movies" tab to "Ratings", added Anime/Western/Movies sub-tabs mirroring the Tier List pattern, and extended the read-only ★ rating badge on Tier List chips to all categories (was movie-only). Updated `CLAUDE.md`'s ratings-export rule to match. Verified live via Playwright.
- Brainstormed and scoped the Supabase backend (previously deferred): started as a single-user narrow-scope plan, then pivoted mid-design after Karl's partner Liisa reviewed the app and asked for her own space plus a shared "Together" list, a mood-tagged shared watchlist, and a Letterboxd import — expanded into a full multi-user design (accounts, three-scope data model, RLS). Wrote a design spec and a 10-task implementation plan, both committed under `docs/superpowers/specs/` and `docs/superpowers/plans/`.
- Walked Karl through creating the Supabase project, the two "explicit grants only" project settings, and later the Auth user invite + SQL Editor migration run — all manual steps only he could do.
- Executed the plan via `subagent-driven-development` in an isolated worktree (Karl's explicit choice, despite an existing memory generally recommending direct implementation for this project's scale — matches that memory's own carved-out exception for "larger or higher-stakes plans"). All 10 tasks shipped and reviewed clean, with a pre-flight scan catching two real plan defects before any code was written (a duplicate variable declaration, a pin-function signature collision) and three mid-implementation fixes: invalid Postgres RLS syntax, missing table GRANTs, and — the significant one — a live security gap where the anon key could write to Together-scope data without signing in, found via an implementer's UI observation and fixed both live (by Karl) and in the repo.
- Final whole-branch review (opus) verified the security fix was complete, then found and fixed 5 more issues in one consolidated pass: a cold-load auth/cache race that rendered Karl's ratings empty until a manual reload, zero error-checking across 7 write call sites, a signed-out-write-controls-render-enabled UX gap, a scope-blind + unchecked-write risk in the not-yet-run Letterboxd import script, and lost domain knowledge (category-by-format rule, runtime_minutes requirements) in a docs rewrite. Two design-level gaps were deferred with explicit rulings rather than fixed: `shows.id` can't hold the same title in two scopes, and the Together tier list isn't actually cross-device-shared (still `localStorage`).
- Merged to `master` locally (matches this project's established direct-to-master pattern; no PR — solo project, already reviewed extensively) — but held the push until Karl tested a real signed-in session locally first, since no subagent in this session had email access to complete a live magic-link login. Karl confirmed shows/scope-switcher/pin/progress-writes all worked correctly; empty ratings were expected (never migrated, since `data.json` never actually had any). Pushed to `origin/master`.
- Updated `todo.md`: cleared the two completed items, added the two deferred schema/design gaps from the final review, plus four new items from Karl's session-end message — creating Liisa's account, password-based auth (vs. magic-link-every-time), gating the signed-out view behind a sign-in screen instead of rendering empty tabs, and a new written-media (books/manga/manhwa/webnovels) tracking section to design fresh next session.
- Ran `/capture-workflow`: found no memory actually written this session to evaluate for skill promotion (a gap — should have captured mid-session per the standing trigger). Wrote it retroactively instead: one feedback memory (check the source branch for uncommitted work before creating an SDD worktree — this session's worktree initially missed the Ratings-tab-rename work because it was still uncommitted on `master`) and one technical reference memory (Supabase project URL, key locations, schema shape, scope model) to the project's local memory folder.

## 2026-08-15

- Switched sign-in from magic-link to email+password (Karl had hit the email rate limit live) — Karl's account got a generated password, Liisa's got a proper invite (she sets her own). Gated the signed-out view behind a real "please sign in" screen instead of empty tabs.
- Brainstormed and implemented the written-media (books/manga/manhwa/webnovels) tracking feature end-to-end: design spec, 7-task implementation plan, executed via `subagent-driven-development` in an isolated worktree. New `written_media` Supabase table (separate from `shows` — different progress units), a 5th "Reading" tab (chapter stepper for manga/manhwa/webnovel, page input for books, pinning, ratings/notes via a generalized `kind`-parameterized version of the existing shared rating/note plumbing), and a to-read quick-add list with tag filtering.
- Live database testing during Task 1 caught two real bugs static review couldn't have: a Postgres statement-ordering error (dropping `NOT NULL` on a column still inside its old primary key), and — more significant — that partial unique indexes can't serve as `ON CONFLICT` arbiters for the app's REST-based upserts, which would have silently broken every rating/note save app-wide, not just written-media's. Task 7's self-initiated tag-filter addition also introduced a real regression in the pre-existing shows-side watchlist filtering (an unscoped DOM query picking up the new tag-filter bar's pills), caught by task review and fixed. Final whole-branch review (opus) found and fixed 3 more issues before merge: dead sort/filter controls on the new Reading tab, a migration file that could no longer replay after an earlier fix, and a missing non-idempotency warning in `CLAUDE.md`.
- Seeded 28 real written-media entries after an interactive back-and-forth confirming Karl's actual reading state per title (10 currently-reading, 1 completed, 17 to-read) — including 5 manga marked "reading" at wherever their anime adaptation currently ends rather than real personal progress, flagged for a future UI marker so it isn't misread.
- Merged and pushed. Follow-up fixes after Karl reviewed live: removed a header that was still rendering behind the sign-in gate (should have been hidden entirely, not just the tab content), collapsed a "Sort: Pinned first" option that was a byte-for-byte duplicate of "Sort: % complete" (pinning is already unconditional) on both Watching and Reading tabs, removed a redundant sign-in subtitle, and added book cover images via Open Library (free, no key — Google Books was tried and rejected, its anonymous quota is effectively zero). Manga/manhwa covers are still pending — Jikan itself works, but MyAnimeList was down when attempted.
- Karl caught that several discussed ideas never made it into `todo.md`: the self-serve add-form gap for Liisa (recommended earlier, never tracked), and 3 minor cleanup findings from the final review (lost when the SDD workspace was deleted before they were transcribed). Both recovered and filed. Also confirmed the Letterboxd import script (built in an earlier session) is ready to run, just needs Liisa's actual export.
- Cleaned up: removed ~52 leftover Playwright screenshot/snapshot files, and found (but couldn't remove — Windows file lock, unresolved) an orphaned empty worktree directory at `.worktrees/multiuser-supabase-backend` left over from the original multi-user-backend session.
- Ran `/capture-workflow`: found 2 promotable candidates this session, both written as memory rather than new skills (neither is a discrete invocable workflow) — a collaboration-preference (deferred ideas must be written to the project's tracking file the moment they're raised, not left to conversational memory — the direct cause of the `todo.md` gaps above) and a technical reference (Windows blocks worktree/directory removal if a background dev server still has that path as its cwd — the same file-lock issue hit the `written-media` worktree removal earlier in this session before recurring on the orphaned directory).

## 2026-08-15 (cont.)

- Checked Karl's 20-title reading list against the seeded to-read list from earlier the same day; 11 titles were still missing. Researched and staged metadata for all 11 (Open Library for book covers/authors, web search for page counts and webnovel author/chapter data — MAL/Jikan returned 504 for the entire session, checked repeatedly over roughly an hour and never recovered). Confirmed with Karl on scope for the two trilogy entries (Mistborn, The First Law) — book 1 only, not the full trilogies.
- Confirmed 3 of Karl's other mid-session ideas were already tracked in `todo.md` from the written-media session rather than lost: the manga "anime-end-point" progress note, the ongoing/hiatus/finished series-status marker, and new-release notifications/news. Added a new long-term aspiration item instead: minimizing Claude-mediated adds by finding more free APIs (Karl explicitly framed this as a dream, not a build request).
- Added a remove (✕) button to watchlist and to-read list rows — first delete/destructive capability in the app (previously only additive/status-changing actions existed). Confirms before deleting, respects the existing write-scope permission check, stops propagation so it doesn't trigger the row's own click action.
- Brainstormed (bounded path) and implemented a nav restructure Karl requested after seeing the flat 5-tab bar: now two-level — **TV/Movies | Reading | Stats** on top, with Watching/Tier List/Ratings nested under TV/Movies and a new Reading/Ratings pair nested under Reading. The new Reading→Ratings view (ranked, rated/finished books/manga/manhwa/webnovels) was built by mirroring the existing Ratings tab and pointing it at `written_media`, reusing the star-rating/note-button components that already supported a `media` kind. Removed the Reload button (redundant with existing auto-refresh-on-focus). Sub-tab selection persists per top-level group via `localStorage`. Verified by tracing the logic and a syntax check only — no browser automation tool was available in this environment (no chromium-cli/Playwright/Puppeteer installed) and the app requires a real Supabase sign-in, so this still needs a live look from Karl.
- Inserted 9 of the 11 staged reading-list titles into `written_media` (Karl's scope, `plan_to_read`) — the 2 manga (Vagabond, Kingdom) stayed blocked on the MAL outage. Hit and documented a PostgREST bulk-insert gotcha along the way: every row in a single insert call needs identical keys, so book-only and chapter-only columns both had to be present (padded `null`) on every row regardless of category — saved to the project's technical memory (`supabase-backend-infra.md`) since it'll recur on any future mixed-category `written_media` batch write.
- Added **The Tatami Galaxy** to `shows` (Karl's scope, watching, ep. 5/11) from a one-line update — looked up episode count/studio/genres via web search since Jikan was still down; poster left null pending a backfill.
- Logged a new feature idea to `todo.md`: Mark Finished should prompt for a rating (and optional note) inline rather than requiring a separate trip to the Ratings tab, reusing the existing star/note components.
- Committed and pushed the nav restructure + remove-button work (`1642964`), along with two pre-existing uncommitted items found sitting in the working tree at session start: a small Add-Show-form doc clarification in the original design spec, and a new `CODEX.md` project guide. Left `server.log` (a local dev-server log) out of the commit.
- Ran `/capture-workflow`: no new entries in `collaboration-preferences.md` or the project's `MEMORY.md` this session, so no skill-promotion candidates. Separately added the PostgREST bulk-insert technical memory noted above (not a skill — a narrow technical gotcha).
- Still open for next session: Vagabond/Kingdom manga lookups and The Tatami Galaxy's poster (all blocked on the same MAL/Jikan outage — retry with `curl -s "https://api.jikan.moe/v4/manga?q=Vagabond&limit=1"`), and a live browser check of the nav restructure since none was possible this session.

## 2026-08-16

Long session — MAL/Jikan never recovered, discovered AniList as a reliable first-party alternative early on and used it throughout. Verified last session's nav restructure live via Playwright (now available in this environment) with no bugs found, then worked through nearly the entire `todo.md` backlog in order.

- **Live-verified and polished the nav restructure**: confirmed the two-level TV/Movies|Reading|Stats nav, Reading→Ratings view, and watchlist/to-read remove buttons all work correctly. Then redesigned the nav hierarchy per Karl's feedback that 3 stacked button rows looked confusing: scope switcher (Karl/Liisa/Together) moved into the header as a dropdown; top-level tabs restyled as bold underlined tabs; sub-tabs restyled as small pill chips in a tinted strip; dark/light mode moved to a fixed corner button; fixed the "Karl" username text rendering at browser-default size instead of matching the surrounding UI.
- **Stats tab**: added a Reading section (episode/chapter/page totals, category split, genres), then split it into a Watching/Reading toggle (two panels instead of one long stacked page) per follow-up feedback.
- **Mark Finished now prompts for a rating**: clicking Mark Finished (shows or reading) opens a skippable star-rating + optional-note modal instead of silently flipping status with no rating capture.
- **Switched primary anime/manga/manhwa lookups to AniList** (first-party API, not a Jikan/MAL proxy — doesn't inherit MAL's outages) with Jikan as fallback; documented in `CLAUDE.md`. Backfilled cover art for all 27 previously-uncovered manga/manhwa via AniList (catching one wrong-series match along the way — "Leviathan" needed disambiguation by `countryOfOrigin`) plus 3 pure webnovels via their published Open Library editions. Seeded Vagabond (327 ch, hiatus) and Kingdom (885 ch, ongoing) to close out the last blocked bulk-seed titles, and backfilled The Tatami Galaxy's poster.
- **Discovered and fixed a process gap**: nothing from this session (or apparently much of it) had actually been pushed to `origin/master` — Karl noticed the live GitHub Pages site wasn't reflecting any of the work. Committed and pushed in stages throughout the rest of the session instead of batching to the end.
- **Anime-sync chapter badge**: added `chapter_synced_from_anime` column (migration run by Karl), backfilled `true` for the 5 manga seeded at their anime-adaptation endpoint (Haikyu!!, Hunter x Hunter, The Fable, Blue Lock, Vinland Saga), added a "from anime" badge that auto-clears on the next real chapter bump or finish.
- **Reading tab split into Book/Manga/Manhwa/Webnovel sub-tabs**, mirroring the existing Anime/Western/Movies pattern and replacing the previously-dead "Type" filter dropdown.
- **Series ongoing/hiatus/finished status badge**: added `series_status` column, backfilled for all 27 manga/manhwa via AniList — with a manual correction for Vagabond, since AniList reports it `RELEASING` despite being famously on hiatus since 2015 (its `HIATUS` flag isn't reliable for long-dormant series). Shows as a small badge next to the category label.
- **New-chapter checking**: on Reading-tab render, polls MangaDex's live chapter feed (throttled to once/6h) for actively-reading manga/manhwa and shows a dismissible "New chapters: X (+N)" banner. Had to use MangaDex rather than AniList since AniList's `chapters` field is only populated once a series is `FINISHED` — useless for the ongoing titles this actually needs to catch. Caught and fixed two real bugs during testing: MangaDex's raw top search hit is often a lagging colorized/spinoff edition ("Hunter x Hunter (Official Colored)" outranked the base series), fixed by preferring an exact title match; and the grid could render with pre-update totals since the check runs fire-and-forget, fixed by re-rendering after applying updates.
- **Together tier list/ranked list is now actually shared**: added `tier`/`rank` columns to `shows` (migration run by Karl). When viewing Together scope, placement reads/writes through Supabase instead of per-browser `localStorage`; Karl's/Liisa's personal scopes are unchanged. Verified live with temporary Together-scope test rows that a fresh `renderTierList()`/`renderRankedList()` call — simulating a second device — picks up the shared placement correctly.
- **Fixed `shows.id`'s primary-key collision gap**: changed `shows`' PK from `(id)` to `(id, scope)` so the same slug can exist once per scope (migration run by Karl). Since `ratings.show_id` had a real FK into `shows(id)`, this required adding a `scope` column to `ratings` too and making its FK/uniqueness constraint compound. Audited and scoped every write that previously targeted a `shows` row by bare id (pin, progress bump, mark finished, watchlist remove, stale-pinned cleanup, tier/rank saves, `setRating`/`setNote`) to also filter by scope, since an unscoped write would otherwise silently touch the same-slug row in every scope once duplicates became possible. Verified live: inserted `samurai-champloo` under both `karl` and `together` scopes with no collision, confirmed both rows independent, re-tested pin/unpin to confirm normal writes still work.
- **Self-serve add forms**: replaced the old info-only "+ Add Show" modal with real manual-entry fields (title/category/scope/episode-or-runtime), and added a matching "+ Add Reading" modal — both insert directly via the client (RLS already permits it), so Liisa can now add things herself without going through Claude. Deliberately no lookup automation (core fields only, per Karl's scope call) — poster/genre/author stay blank. Scope dropdowns filter to what the signed-in user can actually write to.
- **Minor cleanup pass**: added a shared `escapeHTML()` and applied it everywhere user-entered text (titles, authors, tags) gets interpolated into `innerHTML` — more relevant now that the add forms make titles genuinely free-typed input, not just Claude-relayed API responses. Verified live with a title containing `<b>`, `&`, and both quote styles. Also surfaced manga/manhwa volume progress in the writable chapter-stepper view (previously read-only-view-only). Left the "shared clear-pinned-on-completion helper" idea alone — it was explicitly scoped as "only worth it if a third pinned table shows up," and none exists.
- **UI polish round**: Stats Watching/Reading toggle now reuses the sub-tabs pill style (fixed both its missing top margin and style-mismatch in one change); category/"done" badges got a bordered-pill treatment instead of blending into plain text; star-rating widget got a small left-margin nudge off the card edge; added a "Viewing X's list — read-only" banner when the active scope isn't yours or Together's — the write restriction already existed (RLS + `canWriteScope()`), this just makes it visible instead of silent.
- **Liisa's account**: confirmed she was already sent a proper invite in an earlier session (explained what the default Supabase invite email looks like so Karl could ask her about it). Then, at Karl's request, set her password directly via the Supabase Admin API instead — `reinupliisa@gmail.com` / verified working sign-in — so she can log in immediately without the invite-link flow.
- Bumped real progress along the way: Dragon Ball Z Kai (Recut) 22→29/47; Dororo moved from watchlist to watching at 1/24 (looked up via AniList — MAPPA/Tezuka Productions).
- `todo.md` now only holds genuinely blocked-on-Karl or explicitly-deferred items: the stuck orphaned worktree directory (Windows file lock, needs Karl to check Task Manager), the Letterboxd import (script ready, waiting on Liisa's CSV export), news-of-new-releases for already-finished series (needs its own design pass — a different, fuzzier problem than the new-chapter checker), and the long-term Claude-mediated-adds aspiration (its own stated trigger condition — "once a self-serve add form exists" — was met this session; flagged to Karl but left as-is pending his call).
- Ran `/capture-workflow` at session end: no promotable candidates — nothing was written to `collaboration-preferences.md` or the project's `MEMORY.md` this session to evaluate.

## 2026-08-16 (cont.)

- **Pibble background for Liisa**: a chubby sleepy French Bulldog puppy photo (free Unsplash, correctly identified as the "pibble" meme after an initial wrong guess at pit bull terriers) shows as a subtle themed-scrim background whenever Liisa is signed in or her scope is active — Karl included, when he views her list.
- **Escalated to MLG-style pibble spam** per follow-up ask ("spam more pibble images... spin the images around... even over the top"): 18 scattered, randomly rotated, partly-spinning copies of the same photo across the full viewport under the same trigger. Caught and fixed a real stacking bug during the first live test — the `position:fixed` spam layer was painting *over* interactive content (dropdowns, filter inputs) instead of behind it, since any explicitly-positioned sibling with a z-index beats normal-flow content regardless of DOM order. Fixed by giving `#app-header`/`#signed-out-screen`/`#app-content` their own higher stacking context. Re-verified live afterward: content fully readable/clickable, a real control (theme toggle) still works, and the whole thing correctly toggles off for Karl's own scope and Together.
- **Header polish**: grouped "Karl" + "Sign Out" into one bordered pill (previously bare text floating next to an unrelated-looking button — flagged as looking "weird").
- **`.gitignore` audit** (Karl asked whether `supabase/` should be excluded): confirmed the actual secrets (`.supabase-service-key`, `.supabase-passwords`) were already gitignored, and the `supabase/*.sql` migration files have no embedded secrets — just standard Postgres role names in `GRANT` statements — so they're fine to keep tracked, same as any migrations folder. Added `server.log` and `.playwright-mcp/` instead, since those were showing up as untracked cruft in `git status` every session.
- Committed and pushed both rounds of this continuation separately.
- Ran `/capture-workflow` again at session end: still no promotable candidates.

## 2026-08-23

- **Movies/watchlist gap**: discovered there was no way to add a movie as "want to watch" at all — the Add Show/Movie flow always force-marked movies watched, and the generic watchlist row assumed episode-1 semantics. Added a Watched/Want-to-watch status choice to Add Movie, and a dedicated card-based **Movie Watchlist** sub-tab (next to In Progress) with a "Watched?" action per card that opens the same rate/note prompt used for finishing a show, plus an explicit Remove button.
- **Add Show vs Add Movie cleanup**: "Movie" used to be a third option in Add Show's Category dropdown, which read oddly once a dedicated Add Movie button existed — split into an explicit mode flag instead: Add Show only offers Anime/Western, Add Movie hides the category picker entirely and shows "Add a Movie" as the title.
- **In-page Letterboxd importer**: built a browser-side CSV importer (ratings/watched/watchlist, typed by filename) that writes straight to Supabase like the rest of the app — Liisa can now import her own export herself without a CLI hop. Extended the existing `scripts/import-letterboxd.mjs` CLI script in parallel with a `--scope` flag as a fallback path.
- **Add-time Image URL field**: Add Show/Movie/Reading modals now take an optional poster/cover URL at creation — covers new entries only, editing existing ones is still unsupported (flagged, not built).
- **Renamed "Watching" → "In Progress"** now that Movie Watchlist sits alongside it as a fourth sub-tab.
- **Data fixes**: marked Nippon Sangoku finished (11/11); backfilled poster/studio/genres for Astra Lost in Space and Nippon Sangoku, and poster/genres for a self-added "Godfather" entry — AniList's API was down all session (403, "temporarily disabled due to severe stability issues") and MAL/Jikan also 504'd, so posters came from Wikipedia/TMDB instead of the usual sources; worth revisiting once AniList recovers. Backfilled a missing cover for "Morning Star" via Open Library.
- **Discussed and declined for now**: a one-click "fetch missing images" button — flagged that blind auto-apply risks wrong matches (the same collision problems already documented in `CLAUDE.md`'s lookup rules), recommended a review-and-approve version instead if revisited later.
- **New CLAUDE.md workflow**: documented an on-demand "audit self-added entries for missing metadata" check (querying both tables for null poster/cover/genres, backfilling via the existing per-category lookup rules) since the website's manual-entry modals never do lookups.
- Verified every UI change locally via Playwright against a static `python -m http.server`, using a fake signed-in state plus a mocked `supabase.from()` to exercise write paths without touching production data (see the new `playwright-mock-supabase-writes` project memory) — no real writes went through Playwright this session, only the direct `supabase-write.mjs`/curl calls for the data fixes above.
- Committed and pushed in stages throughout rather than batching to the end.
- Ran `/capture-workflow` at session end: no promotable candidates — this session's fixes were app-specific product feedback (button wording, tab order, modal split), not cross-project workflows. Saved one technical memory instead (the Playwright/mocked-Supabase verification pattern, since it'll recur on this project).
